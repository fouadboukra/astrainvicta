@distance = 50
@base_moon_distance = 10
@jumps = 3

### Trappist
sol_sector_trappist_initializer = {
	name = "Trappist"
	class = sol_sector_red_dwarf_unary
	flags = { ss_init_unique }
	usage = misc_system_init
	usage_odds = {
		base = 0
	}

	max_instances = 1

	planet = {
		name = "Trappist"
		count = 1
		class = pc_m_star
		orbit_distance = 1
		size = 20
		has_ring = no
	}

	# Trappist b — innermost, extremely hot, lava world
	planet = {
		name = "Trappist b"
		class = pc_molten
		orbit_distance = 40
		size = 10
		has_ring = no
		init_effect = {
			add_modifier = {
				modifier = "tidal_locked"
				days = -1
			}
		}
	}

	# Trappist c — hot rocky world
	planet = {
		name = "Trappist c"
		class = pc_barren
		orbit_distance = 15
		size = 11
		has_ring = no
		init_effect = {
			add_modifier = {
				modifier = "tidal_locked"
				days = -1
			}
		}
	}

	# Trappist d — warm, inner habitable zone edge
	planet = {
		name = "Trappist d"
		class = pc_barren
		orbit_distance = 15
		size = 9
		has_ring = no
		init_effect = {
			add_modifier = {
				modifier = "tidal_locked"
				days = -1
			}
		}
	}

	# Trappist e — habitable zone, most Earth-like
	planet = {
		name = "Trappist e"
		class = pc_continental
		orbit_distance = 15
		size = 12
		has_ring = no
		init_effect = {
			prevent_anomaly = yes
			add_modifier = {
				modifier = "tidal_locked"
				days = -1
			}
		}
	}

	# Trappist f — habitable zone, cooler
	planet = {
		name = "Trappist f"
		class = pc_continental
		orbit_distance = 15
		size = 13
		has_ring = no
		init_effect = {
			add_modifier = {
				modifier = "tidal_locked"
				days = -1
			}
		}
	}

	# Trappist g — outer habitable zone, cold
	planet = {
		name = "Trappist g"
		class = pc_arctic
		orbit_distance = 15
		size = 13
		has_ring = no
		init_effect = {
			add_modifier = {
				modifier = "tidal_locked"
				days = -1
			}
		}
	}

	# Trappist h — outermost, frozen
	planet = {
		name = "Trappist h"
		class = pc_frozen
		orbit_distance = 20
		size = 8
		has_ring = no
		init_effect = {
			add_modifier = {
				modifier = "tidal_locked"
				days = -1
			}
		}
	}
}
